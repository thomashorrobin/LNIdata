@model IEnumerable<YoungNatsDBv1.DataModels.Electorate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ElectorateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ElectorateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ElectorateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ElectorateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ElectorateId })
        </td>
    </tr>
}

</table>
