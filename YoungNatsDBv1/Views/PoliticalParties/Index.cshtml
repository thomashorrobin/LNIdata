@model IEnumerable<YoungNatsDBv1.DataModels.PoliticalParty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PoliticalPartyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PoliticalPartyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PoliticalPartyId })
        </td>
    </tr>
}

</table>
