@model IEnumerable<YoungNatsDBv1.DataModels.PhoneNumber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNumber1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhoneNumberId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhoneNumberId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhoneNumberId })
        </td>
    </tr>
}

</table>
